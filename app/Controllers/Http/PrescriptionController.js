'use strict'

const Prescription = use('App/Models/Prescription')
/** @typedef {import('@adonisjs/framework/src/Request')} Request */
/** @typedef {import('@adonisjs/framework/src/Response')} Response */
/** @typedef {import('@adonisjs/framework/src/View')} View */

/**
 * Resourceful controller for interacting with prescriptions
 */
class PrescriptionController {
  /**
   * Show a list of all prescriptions.
   * GET prescriptions
   *
   * @param {object} ctx
   * @param {Request} ctx.request
   * @param {Response} ctx.response
   * @param {View} ctx.view
   */
  async index ({ request, response, view }) {
  }


  /**
   * Create/save a new prescription.
   * POST prescriptions
   *
   * @param {object} ctx
   * @param {Request} ctx.request
   * @param {Response} ctx.response
   */
  async store ({ request, response }) {

    const data = request.only([ 'patient_id',
                                'sessionDate', 
                                'sessionTime',
                                'prescription',
                                'anamnese',
                              ])

    const prescription = await Patient.create({ doctor_id: auth.user.id, ...data})

    return patient;
  }

  /**
   * Display a single prescription.
   * GET prescriptions/:id
   *
   * @param {object} ctx
   * @param {Request} ctx.request
   * @param {Response} ctx.response
   * @param {View} ctx.view
   */
  async show ({ params, request, response, view }) {
  }


  /**
   * Update prescription details.
   * PUT or PATCH prescriptions/:id
   *
   * @param {object} ctx
   * @param {Request} ctx.request
   * @param {Response} ctx.response
   */
  async update ({ params, request, response }) {
  }

  /**
   * Delete a prescription with id.
   * DELETE prescriptions/:id
   *
   * @param {object} ctx
   * @param {Request} ctx.request
   * @param {Response} ctx.response
   */
  async destroy ({ params, request, response }) {
  }
}

module.exports = PrescriptionController
